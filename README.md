Add structured logging to my Flask backend and React frontend.

Backend:
- Use Python logging with levels
- Log API requests, response time, errors, and SSO flow
- Output to console and file with rotation
- Add middleware for request logging

Frontend:
- Create logger utility (log, info, warn, error)
- Log API calls, errors, and SSO flow
- Enable detailed logs only in development

Follow best practices and avoid sensitive data logging.
